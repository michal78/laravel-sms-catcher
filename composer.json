{
    "name": "michal78/laravel-sms-catcher",
    "description": "A development-only inbox for inspecting Laravel SMS notifications.",
    "keywords": ["laravel", "sms", "notifications", "development", "debugging", "mailpit"],
    "homepage": "https://github.com/michal78/laravel-sms-catcher",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Michal Skogemann"
        }
    ],
    "support": {
        "issues": "https://github.com/michal78/laravel-sms-catcher/issues",
        "source": "https://github.com/michal78/laravel-sms-catcher"
    },
    "require": {
        "php": "^8.2",
        "illuminate/support": "^10.0|^11.0|^12.0|^13.0",
        "illuminate/notifications": "^10.0|^11.0|^12.0|^13.0"
    },
    "require-dev": {
        "laravel/pint": "^1.18",
        "larastan/larastan": "^2.9|^3.0",
        "orchestra/testbench": "^8.0|^9.0|^10.0|^11.0",
        "phpunit/phpunit": "^10.5|^11.0|^12.0"
    },
    "autoload": {
        "psr-4": {
            "SmsCatcher\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "format": "pint",
        "format:test": "pint --test",
        "analyse": "phpstan analyse"
    },
    "extra": {
        "laravel": {
            "providers": [
                "SmsCatcher\\SmsCatcherServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true,
        "audit": {
            "ignore": {
                "CVE-2026-48019": "CRLF injection in Laravel's default email validation rule (illuminate/mail); this package never uses that rule, illuminate/mail is only pulled in transitively via illuminate/notifications for CI compatibility testing."
            }
        },
        "policy": {
            "advisories": {
                "ignore-id": {
                    "CVE-2026-48019": "CRLF injection in Laravel's default email validation rule (illuminate/mail); this package never uses that rule, illuminate/mail is only pulled in transitively via illuminate/notifications for CI compatibility testing."
                }
            }
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
